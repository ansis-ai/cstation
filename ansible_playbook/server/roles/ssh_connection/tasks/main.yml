#SPDX-License-Identifier: MIT-0
---
# tasks file for ssh_connection
- name: Check Server Connection
  ansible.builtin.ping:
    # register: command_output

  # - name: Print command output
  #   debug:
  #     var: command_output.stdout_lines