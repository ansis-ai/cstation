# cstation CLI

<!-- Optional: Add badges here (e.g., PyPI version, license, build status) -->
<!-- [![PyPI version](https://badge.fury.io/py/cstation.svg)](https://badge.fury.io/py/cstation) -->
<!-- [![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT) -->

A brief description of what the `cstation` CLI tool does. Explain its purpose and main features.

## Prerequisites

*   Python 3.x
*   pip (Python package installer)

## Installation

Provide instructions on how users can install your CLI.

**From Source (Recommended for development):**

1.  Clone the repository:
    ```bash
    git clone <your-repo-url>
    cd cstation
    ```
2.  Install the package. For development, use the `-e` flag for an editable install:
    ```bash
    pip install -e .
    ```
    Or for a standard installation:
    ```bash
    pip install .
    ```

**(Optional) Installation via PyPI (if published):**
```bash
pip install cstation
```